[project]
name = "banto"
version = "0.5.2"
description = "番頭 — lightweight compute steward for home AI fleets: health, GPU-politeness, model spin-up, failover LB"
license = {text = "MIT"}
requires-python = ">=3.10"

[project.scripts]
banto = "banto:main"
